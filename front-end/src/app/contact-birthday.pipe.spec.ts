import { ContactBirthdayPipe } from './contact-birthday.pipe';

describe('ContactBirthdayPipe', () => {
  it('create an instance', () => {
    const pipe = new ContactBirthdayPipe();
    expect(pipe).toBeTruthy();
  });
});
